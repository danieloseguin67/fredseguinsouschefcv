name: Deploy Angular app

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: fredseguincv/package-lock.json

      - name: Install dependencies
        working-directory: fredseguincv
        run: npm ci

      - name: Build Angular app
        working-directory: fredseguincv
        run: npm run build:prod

      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Angular outputPath is "../docs" (see fredseguincv/angular.json),
          # so after building in ./fredseguincv the static site is at ./docs
          path: docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
